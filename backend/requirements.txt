# FinancePilot AI — backend
# Render/배포 환경은 Python 3.12 권장 (runtime.txt). Python 3.14는 SQLModel과 충돌할 수 있음.

fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlmodel==0.0.22
pydantic>=2.7.0,<2.11.0
requests==2.32.3
python-dotenv==1.0.1

# 참고: Groq/Gemini/OpenAI 분석 클라이언트는 requests만 사용 (추가 LLM SDK 불필요)
